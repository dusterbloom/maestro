# orchestrator/requirements.txt

# --- Core Application Framework ---
# We keep these as our primary, direct dependencies.
fastapi
uvicorn[standard]
ollama
genai-processors

# --- Supporting Libraries ---
# We remove the strict version pins (e.g., ==2.5.0) to allow pip
# to resolve conflicts. The libraries above will pull in compatible
# versions of these dependencies automatically.
httpx
requests
websockets
python-multipart
numpy
pydantic
python-json-logger